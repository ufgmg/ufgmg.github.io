## Setting up local Jekyll for testing

1.  Install Ruby and Bundler (`gem install bundler`).

2.  ```bash
    $ bundle install --path .bundle
    ```

To test, run

```bash
$ bundle exec jekyll serve
```
